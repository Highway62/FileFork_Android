<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".Forks">


    <!-- Action Bar Forks Button -->
    <item
        android:id="@+id/action_forks_btn"
        android:icon="@drawable/ic_action_forks"
        android:title="@string/action_forks"
        app:showAsAction="always" />

    <!-- Action Bar Devices Button -->
    <item
        android:id="@+id/action_devices_btn"
        android:icon="@drawable/ic_action_devices"
        android:title="@string/action_devices"
        app:showAsAction="always" />

    <!-- Action Bar Contacts Button -->
    <item
        android:id="@+id/action_contacts_btn"
        android:icon="@drawable/ic_action_contacts"
        android:title="@string/action_contacts"
        app:showAsAction="always" />

    <!-- Action Bar Settings button -->
    <item
        android:id="@+id/action_settings_btn"
        android:icon="@drawable/ic_action_settings"
        android:title="@string/action_settings"
        app:showAsAction="always" />

</menu>